# CLAURST Documentation

## Table of Contents
1. [Overview and Purpose](#overview-and-purpose)
2. [Repository Structure](#repository-structure)
3. [Architecture and Components](#architecture-and-components)
4. [Key Systems and Advanced Features](#key-systems-and-advanced-features)
5. [Setup and Run Instructions](#setup-and-run-instructions)
6. [Testing Strategy](#testing-strategy)
7. [Next Steps](#next-steps)

## Overview and Purpose
CLAURST is a clean-room Rust reimplementation of Claude Code (Anthropic's official AI coding CLI assistant). It behaves functionally identical to the proprietary TypeScript source by following detailed behavioral specifications (found in the `spec/` directory) rather than a direct translation of code. The project provides an efficient, memory-safe, fast terminal AI coding agent using Tokio, Ratatui, and a multi-agent orchestrated infrastructure, while incorporating experimental and unreleased capabilities like the "Buddy" Tamagotchi companion, "Kairos" continuous logging, and the AutoDream memory consolidation engine.

## Repository Structure
The repository is split into various distinct directories:
- **`src-rust/`**: The core Rust implementation structured as a Cargo workspace with 12 specific crates.
- **`spec/`**: Contains 13 exhaustive markdown files detailing the architecture, data flows, tool contracts, and behavioral designs generated by an AI analyzing the original codebase.
- **`.github/`**: Houses CI/CD configuration files (such as `release.yml` and `pages.yml`). Contains matrix builds supporting Windows, Linux, and macOS natively and via Cross (for aarch64 dependencies).
- **`public/`**: Stores public images and screenshot assets.

## Architecture and Components

The Rust codebase (`src-rust/`) is structured into a Cargo workspace comprising 12 primary crates:

| Crate | Purpose | Key Responsibilities |
|---------|---------|-----------------------|
| **`claurst-cli`** | Entry Point | Resolves command-line inputs, configures logging, and bootstraps the CLI. |
| **`claurst-core`** | Utilities & Types | Stores basic project constants, models, errors, configurations, utilities, and Undercover Mode logic. |
| **`claurst-commands`** | Slash Commands | Handlers for CLI commands like `/connect`, `/Rocky` (experimental speech types), and `/help`. |
| **`claurst-tools`** | AI Tool Registry | Executes the 40+ system tools: Bash execution, Web fetching, Glob, File utilities, and more. |
| **`claurst-api`** | External API | Negotiates connection parameters, manages `reqwest` instances for streaming connections to Anthropic servers, and handles fast-mode ("Penguin"). |
| **`claurst-query`** | Coordinator | Core business logic handling the Multi-Agent Orchestration ("Coordinator Mode") and memory-consolidation tasks natively. |
| **`claurst-tui`** | Terminal UI | Rich custom terminal user interface driven by `ratatui` and `crossterm`. Uses markdown enhancements. |
| **`claurst-mcp`** | MCP Server/Client | Full implementation of the Model Context Protocol to talk with compatible local and remote MCP hosts. |
| **`claurst-buddy`** | Companion System | The virtual Tamagotchi "companion pet" gacha logic (`Mulberry32` PRNG based). |
| **`claurst-plugins`**| System Extension | Pluggable module architectures allowing further workspace extensions. |
| **`claurst-acp`** | Computer Use | Anthropic Computer Protocol handler inside the terminal (known internally as "Chicago"). |
| **`claurst-bridge`**| Relay Network | External bridging configuration connecting `claude.ai` UI with local telemetry via websocket/JWT relay. |

### Major Dependencies
- **Async Runtime**: `tokio` (with stream support)
- **Networking**: `reqwest`, `tokio-tungstenite`
- **UI & Interactivity**: `ratatui`, `crossterm`, `clap`
- **Security & Hashing**: `sha2`, `rusqlite`, `uuid`

## Key Systems and Advanced Features

The project incorporates several feature-gated systems originally discovered via source leaks matching specific internal testing domains:

- **Buddy**: A Tamagotchi-style pet that watches terminal inputs. Gated by `BUDDY`, using an obfuscated species set with procedurally generated behavioral traits (SNARK, WISDOM) and physical rendering capabilities across the TUI.
- **AutoDream System**: A 4-stage "dreaming" process where the AI takes a background context phase and compacts working memory via a subagent. It relies on a three-gate trigger system (Time, Session, Lock).
- **Kairos Mode**: An "Always-On" proactive background system equipped with exclusive tools (`SendUserFile`, `PushNotification`) acting entirely implicitly after monitoring log modifications.
- **UltraPlan**: Spawns remote CCR (Cloud Container Runtime) instances taking upwards of 30 minutes for deep-reasoning execution plans, returning actionable code directly back via synchronous `__ULTRAPLAN_TELEPORT_LOCAL__` signals.
- **Multi-Agent Teams**: By enabling "Coordinator Mode", a master agent spawns swarms of async Rust tasks that can delegate discrete codebase research autonomously across different workspace files simultaneously.
- **Undercover Mode**: Analyzes current git remotes to protect proprietary paths or feature names (e.g. Model names like "Tengu", "Capybara") natively without requiring `.npmignore` configuration.

## Setup and Run Instructions

### Prerequisites
1. **Rust Toolchain**: You will need Rust version `1.70` or newer installed via [rustup](https://rustup.rs/).
2. **System Dependencies**: You must install base native dependencies to compile the SSL and Audio crates properly.
   - Ubuntu/Debian:
     ```bash
     sudo apt-get update
     sudo apt-get install -y libasound2-dev libssl-dev pkg-config
     ```
   - macOS:
     ```bash
     brew install openssl pkg-config
     ```

### Building the Project
Clone the repository, transition to the `src-rust` directory, and utilize Cargo to build:

```bash
cd claurst/src-rust
# Compile locally with release optimizations
cargo build --release

# Run the command-line interface
./target/release/claurst
```

*Note: Experimental feature gates can be toggled using Cargo features during compilation if manually implemented within the respective crates.*

## Testing Strategy

The repository contains basic foundational `ratatui` visual pipeline tests located under `crates/tui/tests/`. However, extensive test coverage is missing logic modules, tools, API connectivity, and command modules. 

A thorough recommended testing strategy to adopt:

1. **Unit Testing Tool Functions (`claurst-tools`)**
   - Write comprehensive boundary conditions testing the 40+ independent system tools. 
   - Mock file system paths explicitly to test `FileReadTool`, `GrepTool`, `BashTool` logic without side effects.
   - Example to add inside `crates/tools/src/todo_write.rs`:
     ```rust
     #[test]
     fn test_todo_write_creates_file() {
         // test logic against a tmp directory
     }
     ```

2. **API Interaction Tests (`claurst-api`)**
   - Mock standard server JSON responses simulating local `reqwest_eventsource` loops, tracking "Penguin mode" fast API invocations securely.

3. **Multi-Agent Concurrency Testing (`claurst-query`)**
   - Validate whether "Coordinator Mode" correctly awaits concurrent worker signals. 
   - Ensure the locking mechanisms inherent in the `AutoDream` memory compaction block effectively prevent redundant background sub-agents from overlapping.

4. **Continuous Integration (CI)**
   - Expand `.github/workflows/` beyond building release targets.
   - Integrate a `cargo test --workspace` stage inside a new GitHub Action workflow prior to `release.yml`. Ensure all native packages are accessible to the runner.

## Next Steps

1. **Improve Code Coverage**: Expand testing strategies defined above utilizing `cargo tarpaulin` to evaluate coverage metrics directly.
2. **Abstract Security Policies**: Formalize the path traversal and Undercover boundaries logic more securely.
3. **Extend Computer Use**: Document capabilities and implementation details for the "Chicago" (`acp`) protocol connecting terminal workflows with local OS abstractions.
